/*
 Template for the local, site specific configuration.

 Copy this file to "secrets.h" (same directory) and enter your own values.
 "secrets.h" is excluded via .gitignore and must never be committed.
*/
#pragma once

//-- WIFI ---------------------------------------------------------------------------

#define WIFI_SSID     "<enter here>"
#define WIFI_PASSWORD "<enter here>"

//-- Image server ---------------------------------------------------------------------

// Host and port of the server, that receives the images and delivers the settings
#define SERVER_HOST "192.168.178.87"
#define SERVER_PORT "9001"
